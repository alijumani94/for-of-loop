let Name= "AliJumani";
let count= 0;
for(let alphabet of Name){
    count++
    console.log(`Alphabet ${count}= `+ alphabet);
        
}

console.log("Total number of alphabets are: "+count);
